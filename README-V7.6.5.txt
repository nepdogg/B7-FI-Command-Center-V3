B7 FI Command Center V7.6.5

Changes:
- Tool Type dropdown now uses the same navigation-control geometry, hover, pointer cursor, border and active behavior as adjacent page controls.
- Main navigation pictograms normalized to the prototype scale and spacing.
- All Command Center centers/pages now share a bounded 606px workspace with page-level scrolling instead of clipping/overflowing the shell.
- Operations Tools and Tool Editor pages scroll inside the workspace so UTC bottom sections remain reachable.
- Footer remains five equal sections with the four universal user badges filling the final section and no outer user-grid border.
- Preserves V7.6.4 render fix and multi-user functionality.
