B7 FI Command Center V7.5.8 — Prototype Canvas Lock

Desktop now uses a single 1920x1024 prototype canvas that uniformly scales to the available browser viewport. This prevents browser zoom from independently compressing the UTC, status carousel, header, and footer. The three-section header remains structural. Footer version contains only V7.5.8.
