B7 FI COMMAND CENTER V7.6.0 — CLEAN PROTOTYPE REWRITE

This build replaces the loaded visual layer with one new stylesheet:
  css/v7.6.0-clean-prototype.css

Legacy CSS files remain in the archive for reference but index.html does NOT load them.
No JavaScript browser-zoom compensation is used for normal desktop mode.
The browser's native zoom controls the complete page.

Target: approved prototype supplied 09/20/2026.
Desktop geometry is intentionally kept together rather than independently compressed.
